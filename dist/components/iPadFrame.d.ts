import React from 'react';
import '../styles/styles.css';
import { DeviceFrameProps } from '../types';
export default function IPadFrame({ screenshotList, phoneColor, buttonColor, buttonTextColor, statusBar, }: DeviceFrameProps): React.JSX.Element;
