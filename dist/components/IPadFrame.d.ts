import React from "react";
import "../styles/styles.css";
import { DeviceFrameProps } from "../types";
export default function IPadFrame({ screenshotList, deviceColor, buttonStyles, orientation, statusBar, }: DeviceFrameProps): React.JSX.Element;
